# NOTES

mingw-make 对 unix 命令的兼容性

> 故意不小心只支持了部分, 这样你才知道用的是 mingw-make

`cd`: 完全支持

对于
`mkdir`
`rm`
`cp`
`mv`
`touch`
`file`
等命令
参数中直接或间接含有路径时(尤其是 unix 路径格式), 必须使用`""`包裹
含有多个路径时, 只要其中一个使用了`""`, 命令就能正常执行

> [!IMPORTANT]
> 如果需要统一格式, 则建议所有路径都使用`""`包裹
